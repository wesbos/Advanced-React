# Client facing API where we will do most of the querying for the app
# how you import in GraphQL below => will only import things that are used.. (done with prisma)
# import * from './generated/prisma.graphql'

type Mutation {
    createItem(title: String, description: String, price: Int, image: String, largeImage: String): Item!
}

type Query {
    items: [Item]!
}